# Copy this to .env.local and set your Piped API instance URL
# Public instances are mostly offline as of 2026 — self-host or use a known working one
# See BACKEND_STATUS.md for options
VITE_PIPED_API_URL=https://your-piped-instance.example.com
